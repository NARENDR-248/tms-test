const DelaysIndicator = ({ bColor }: any) => {
  return (
    <div
      style={{
        width: "18px",
        height: "10px",
        borderRadius: "100%",
        backgroundColor: bColor,
      }}
    ></div>
  );
};

export default DelaysIndicator;
