import { styled } from "@mui/material";
import Tab from "@mui/material/Tab";

const CameraTabsStyles = {
  StyledTab: styled(Tab)(({ theme }) => ({
    textTransform: "none",
  })),
};

export default CameraTabsStyles;
